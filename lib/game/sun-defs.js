ig.module( 
	'game.sun-defs' 
)
.requires(
	
)
.defines(function(){

SunDefs = {
	
	YellowSun: {
		tilesetOffset: 0,
		innerRadius: 0,
		outerRadius: 700,
		radius: 700,
		innerColor: 'rgba(255,255,220,0.6)',
		outerColor: 'rgba(255,255,255,0.0)'
	}
	
};
	
});
